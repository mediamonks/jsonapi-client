{
  "name": "jsonapi-client",
  "version": "3.0.0-alpha",
  "description": "TypeScript JSON:API formatter and client with type safe sparse fieldsets and compound documents",
  "main": "dist/index.js",
  "author": "Nathan Uphoff",
  "license": "MIT",
  "scripts": {
    "start": "yarn && yarn watch",
    "watch": "tsc -w -d --skipLibCheck",
    "build": "rimraf dist && tsc -d --skipLibCheck",
    "test": "NODE_ENV=development jest --no-cache --watch --coverage",
    "test:ci": "NODE_ENV=development jest",
    "prepublishOnly": "yarn build"
  },
  "dependencies": {
    "isntnt": "^1.4.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.13",
    "jest": "^26.4.2",
    "node-fetch": "^2.6.1",
    "prettier": "^2.0.5",
    "ts-jest": "^26.3.0",
    "typescript": "^4.1.3",
    "url-parse": "^1.4.7"
  }
}
