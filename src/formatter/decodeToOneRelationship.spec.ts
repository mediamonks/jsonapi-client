describe('decodeToOneRelationship', () => {
  it.todo('returns a success validation if every included resource is decoded without errors')

  it.todo('returns a failure validation if a required field equals null')

  it.todo('returns a failure validation if some included resource is decoded with errors')

  it.todo('returns a failure validation if some resource is not found in the included resources')
})
