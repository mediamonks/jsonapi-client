describe('validation', () => {
  describe('success', () => {
    it.todo('returns a success validation with the provided value as its value')
  })

  describe('failure', () => {
    it.todo('returns a failure validation with an array of validation errors/feedback')
  })
})
