import { Predicate, Static } from '../types';
export declare const shape: <T extends Record<string | number | symbol, Predicate<any>>>(shape: T) => (value: any) => value is { [K in keyof T]: Static<T[K]>; };
